# QuantShield — Python backend dependencies
# Install with: pip install -r requirements.txt

# Web framework & server
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data validation
pydantic==2.10.3

# Market data — pinned to 1.x which is the current stable major
# DO NOT downgrade to 0.2.x; it has the YFTzMissingError on certain hosts
yfinance==1.4.1

# HTTP session (used by engine.py for crumb auth + direct API fallback)
requests==2.32.3

# Numerical / data
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1

# No plotting libraries — this is a headless API
